# Copy to .env and fill in local values. Never commit real secrets.
ENVIRONMENT=development
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/freight
# Set to true only when the PostgreSQL server has the TimescaleDB extension available.
TIMESCALE_ENABLED=false
CORS_ORIGINS=http://localhost:3000
